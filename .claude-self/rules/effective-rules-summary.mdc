---
description: Consolidated QA automation rules (under 200)
alwaysApply: false
---

# Effective QA Rules (Condensed)

## Scope And Risk

- Prioritize regressions that can affect production behavior, especially auth, permissions, status transitions, money values, and bulk actions.
- Prefer deterministic validations over timing-only checks.
- Treat selector changes as high-risk and include test impact notes.

## Project Structure

- Keep Gherkin scenarios in `features/*.feature`.
- Keep step logic in `features/steps/*.steps.ts`.
- Keep shared hooks/world in `features/support/`.
- Keep reusable constants and XPath locators in `properties/*.ts`.
- Use TypeScript for support, hooks, and step files.

## Navigation And Waiting

- Use resilient sidebar locators anchored by stable attributes such as `@title`.
- Ensure parent menus are expanded before clicking submenu links.
- Prefer normal click, fallback to `dispatchEvent("click")` only when needed.
- Use `scrollIntoViewIfNeeded()` before click attempts on sidebar items.
- Assert `waitForURL(...)` after navigation clicks.
- Prefer `waitForURL`, `waitForLoadState`, and visible/attached checks over sleeps.
- In headless runs, keep a desktop viewport for dashboard stability.

## Selectors And Self-Heal

- Use XPath selectors in page property files.
- Use plain constants or `Record<string, string>` maps for locator lookup.
- Self-heal only locator drift, not business-logic/assertion failures.
- Model self-heal with ordered fallback XPaths (2-3 alternates max).
- Resolve selector candidates by first unique match (`count === 1`), otherwise fail fast.
- Log fallback usage behind explicit `.env` flags.
- Remove stale fallback selectors during maintenance.

## Assertions And Validation

- Ensure assertion target XPaths resolve to expected uniqueness before interaction.
- Keep assertion messages short, clear, and reusable.
- For table-driven checks, map user-facing names to property-file locator keys.
- Add at least one failure-path assertion when adding new positive flow coverage.

## Export Flow Standards

- Reuse a consistent flow for export validation:
- `Page Ready -> Export -> View Exports -> Refresh until ready -> Download -> Parse -> Assert data`.
- Keep export/download utility logic shared; keep metric-specific logic in dedicated steps.
- Assert exported files contain data rows (not just file existence).

## Logging And Diagnostics

- Gate verbose logs with `.env` flags.
- Use stable, grep-friendly debug output format for scenario-row logs.
- Sanitize debug table values for plain-text logging.
- For selector investigation, check `html/` first; if missing, capture and save `htmlBody` snapshots to `html/` with deterministic page-based filenames (no timestamp suffix).

## Cross-System Context

- Consider whether behavior belongs to the frontend UI, the backend API, or cross-system integration.
- Document cross-system dependencies when scenario behavior depends on sibling apps.

## Documentation Hygiene

- Update `README.md` when setup, scripts, dependencies, or run flow changes.
- Update `changelogs.md` for coverage/behavior/process changes.
- Keep changelog newest-first.
- Use one date section per day in changelog and append same-day updates under it.

## Gherkin Writing — Reuse Step Text First (Step 6)

- Before generating any Gherkin, read `tests/steps/*.steps.ts` and extract all known step patterns.
- Write scenario steps using exact existing step text wherever the intent matches — never paraphrase.
- After generating all Gherkin, run `npx bddgen` dry-run and save unmatched steps to `6_unmatched_steps.txt`.
- Only unmatched steps (lint errors) carry forward to Step 7. Everything else is already done.

## Step Implementation — One Step At A Time (Step 7 and all automation writing)

This rule applies to every automation writing activity without exception.

- **Gap list only**: Step 7 reads `6_unmatched_steps.txt`. Steps not in that list are skipped — they already have implementations.
- **One at a time**: implement exactly one unmatched step per cycle. Never write two steps simultaneously.
- **Verify**: run `npx bddgen` after each implementation. Retry once on failure; stop the TC on second failure.
- **Prove it**: run `npx playwright test --grep @TC-XX`. Do not advance until the step passes in context.
- **Record on pass**: log "★ recorded: <step text>"; remove from unmatched list; move to next step.
- **Stop on fail**: halt the TC. Do not skip ahead. Do not record unproven steps.

The library grows one confirmed step at a time. Recorded steps feed back into Step 6's reuse check on the next ticket — shrinking the gap list permanently.

## Engineering Principles

- Keep implementations KISS and readable.
- Avoid brittle selectors and sleep-heavy tests.
- Double-check changes with targeted validation.
