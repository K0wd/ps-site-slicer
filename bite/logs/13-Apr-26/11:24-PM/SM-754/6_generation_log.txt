All three files have been written. Here's a summary of the changes:

**Properties file** (`purchasing-tracker.properties.ts`):
- Replaced generic `//table` and `//th` XPaths with ag-Grid-specific selectors using `col-id`, `@role='columnheader'`, `.ag-header-cell-menu-button`, `.ag-filter-icon`, etc.
- Added `COL_IDS` map connecting human-readable column names to ag-Grid `col-id` values (verified from HTML: `fsm_po`, `fcreatedstart`, `fneededbystart`, `fwo`, `frequested`, `fdivision`, `ftype`, `fdescription`, `fstatus`; offscreen columns marked with TODO)
- Added ag-Grid filter popup selectors for text, set (checkbox), date, and number filter types

**Feature file** (`purchasing-tracker.feature`):
- 22 scenarios covering all 17 test cases + key edge cases
- Covers: ID, Request Date, Request By Date, WO#, Requested Total, Division (no filter), Type, Description, Status, Approval (dropdown verification + Approved/Blank), Approver(s) (+ Blank), Assigned To, Dept (+ clear behavior), Priority (+ Blank), Vendor, PO#, Needs My Approval, and multi-filter

**Steps file** (`purchasing-tracker.steps.ts`):
- Uses `getColId()` helper to resolve column names to ag-Grid `col-id` attributes
- Separate step definitions for text filters, number filters, date range filters, and set filters (checkbox dropdowns)
- Proper ag-Grid popup menu wait pattern (`ag-menu` visibility check)
- All selectors imported from properties file — no hardcoded XPaths in steps
